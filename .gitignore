.idea
target
client/web/node_modules
node_modules
localstack-data
client/web/dist
.env
states
.DS_Store
replays/

# Sync flight-recorder traces (see DEBUGGING.md)
traces/
loadtest-reports/
.video-cache/
# Captured clip masters are lossless build artifacts — 156 MB and growing,
# in a repo with no LFS. The reproducible part is checked in: the scenario
# JSON that produced each one, and the clip contract under
# .claude/skills/snaketron-create-video/assets/launch-trailer/clip-contracts/.
# Re-capture with tools/video/capture.mjs / scripts/capture_card.mjs.
tools/video/clips/*/master.mkv
__pycache__/
*.pyc

# Machine-readable staging certification bundles.
test-results/autoscaling-*/
